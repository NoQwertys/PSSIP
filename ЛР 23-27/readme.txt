Запустить решение > найти файл appsettings.json > отредактировать строку с DefaultConnection > вписать свой логин, пароль и название БД из MySQL Workbench (без этих данныех не будет подгружаться таблицы из БД)

Так же в этом же файле поменять настройки SMTP в строке SmtpSettings > введите свои данные (без этих данныех не будет работать отправка писем на почту)